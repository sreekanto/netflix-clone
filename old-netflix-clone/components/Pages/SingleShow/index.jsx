import React, { Fragment } from "react";
import { Banner } from "../../";
const SingleShow = (props) => {
  console.log(props);
  return (
    <Fragment>
      <Banner />
    </Fragment>
  );
};

export default SingleShow;
