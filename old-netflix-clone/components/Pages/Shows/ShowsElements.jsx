import styled from "styled-components";
import { Col, sectionPading } from "../../globalElements";

export const ShowsElement = styled.section`
  padding-top: ${sectionPading};
  padding-bttom: ${sectionPading};
  ${Col} {
    padding-bottom: 0.5rem;
  }
`;
