import React, { Fragment } from "react";
import { LoadingElement } from "./LoadingElements";

const Loading = () => {
  return (
    <Fragment>
      <LoadingElement />
    </Fragment>
  );
};

export default Loading;
